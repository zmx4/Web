1. 计算器,计时器,倒计时器,时钟(显示当前系统时间),日历
2. 加密工具,使用趣味方式对文本或文件进行加密解密
3. 编码辅助工具,支持base64与各类编码进行转换
4. 登录页面（用户名、密码、提交、重置表单控件）
5. 注册表单（至少包含用户名、密码、确认密码、提交、重置表单控件）
## 要求
1.	HTML专注于内容的设计，CSS负责设置内容的样式
2.	用CSS+DIV的方式布局网页整体架构，网页局部内容可采用表格等多种布局方式。
3.	对用户在登录、注册表单中提交的数据进行合理的验证。
4.	页面外观简单、大方、美观，页面内容充实；
5.	用超链接把网站所有的页面链接成有机的整体。
6.	网站的文件夹层次结构清晰，资源分类存放。
7.  网站为静态网站

# 多功能工具集 (Multi-function Tool Set) 项目讲解

## 1. 项目概述
本项目是一个集成了多种实用工具的静态Web应用。它旨在提供一个简洁、高效的在线工具箱，主要功能包括：
- **计算器**：支持基础运算。
- **计时工具**：包含秒表、倒计时、实时时钟和日历。
- **加密/解密**：支持凯撒密码、摩斯密码和反转加密。同时也包括了一个趣味加密使用emoji表情对文本进行加密.
- **编码转换**：支持Base64、URL编码和Unicode编码。
- **用户系统模拟**：包含登录和注册页面的前端逻辑及验证。

项目采用原生 HTML5, CSS3 和 JavaScript (ES6+) 开发，无外部框架依赖，展示了扎实的前端基础。

---

## 2. 关键代码解析

### 2.1 加密算法实现 (`js/encryption.js`)
项目中实现了几种经典的加密算法。其中**凯撒密码 (Caesar Cipher)** 的实现逻辑较为完整，处理了大小写字母的偏移，并保持非字母字符不变。

```javascript
// 凯撒密码加密核心逻辑
function caesarEncrypt(text, shift) {
    return text.split('').map(char => {
        if (char.match(/[a-z]/i)) { // 仅处理字母
            const code = char.charCodeAt(0);
            // 判断是大写还是小写，确定基准值 (A=65, a=97)
            const base = code >= 65 && code <= 90 ? 65 : 97;
            // 核心公式：(当前字符编码 - 基准 + 偏移量) % 26 + 基准
            return String.fromCharCode(((code - base + shift) % 26) + base);
        }
        return char; // 非字母字符原样返回
    }).join('');
}
```
2.2 编码工具的中文支持 (encoding.js)
在进行 Base64 编码 时，直接使用 btoa 处理中文字符会报错。项目使用了 encodeURIComponent 和 unescape 的组合技巧来完美支持 UTF-8 字符（如中文）。

2.3 视觉动画效果 (animations.js)
项目包含了一些提升用户体验的视觉效果，例如自定义光标跟随。这里使用了线性插值 (Lerp) 算法，使光标的移动具有平滑的惯性效果，而不是生硬地吸附在鼠标位置。

2.4 计时器逻辑 (timer.js)
秒表的实现使用了 setInterval，并通过状态变量 stopwatchRunning 来控制开始和暂停，防止重复启动定时器。

3. 项目特色与亮点
✨ 1. 交互细节丰富
平滑过渡：页面加载时，工具卡片 (.tool-card) 会依次出现（使用了 animation-delay），营造出流畅的入场感。
自定义光标：摒弃了系统默认光标，使用了一个带有惯性跟随效果的圆形光标，增加了页面的科技感和趣味性。
粒子背景：animations.js 中动态生成了浮动的粒子背景，使静态页面显得不那么单调。
🛡️ 2. 健壮的错误处理
在编码/解码工具中，代码使用了 try-catch 块包裹核心逻辑。例如在 Base64 解码或 URL 解码时，如果用户输入了非法字符串，程序不会崩溃，而是会抛出友好的错误提示。

🧩 3. 模块化的代码结构
尽管没有使用构建工具，但代码结构清晰：

pages 目录存放各功能页面。
js 目录下按功能拆分了 calculator.js, encryption.js, timer.js 等，便于维护和扩展。
style.css 统一管理样式，保持了设计的一致性。
🔐 4. 模拟真实业务逻辑
登录 (login

.js) 和注册 (register.js) 模块虽然是前端模拟，但包含了完整的表单验证（非空、格式检查）和 [localStorage](http://_vscodecontentref_/18) 的使用（"记住我"功能），模拟了真实应用的数据持久化交互。